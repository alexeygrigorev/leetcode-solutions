#include <vector>

using namespace std;

class SelfProductSolution {
public:
    vector<int> product_except_self_two_arrays(vector<int>& nums);
    vector<int> product_except_self_o1_memory(vector<int>& nums);
    vector<int> product_except_self_one_pass(vector<int>& nums);
    vector<int> productExceptSelf(vector<int>& nums);
};
