#include <vector>

using namespace std;

class SingleNumber2Solution {
public:
    int single_number_hashmap(vector<int> &nums);
    int single_number_bits(vector<int> &nums);
    int singleNumber(vector<int> &nums);
};